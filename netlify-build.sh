#!/bin/bash
# Netlify build script
# Static files are pre-generated locally, so this script does nothing

echo "✅ Static files already built locally"
echo "📦 Publishing from: static-site/"
echo "🚀 No build process needed - deploying pre-built static files"

exit 0
